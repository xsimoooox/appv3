import Parametres from './Parametres';

/** Paramètres entendant — même interface que la personne sourde. */
export default function EntendantParametres() {
  return <Parametres variant="hearing" />;
}
